# QBIT Connect — backend dependencies (Phase 2)
# Pinned minimal set; no unnecessary frameworks (Brief §2).

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
email-validator>=2.1       # EmailStr validation (pydantic networks)
pydantic-settings>=2.3

sqlalchemy[asyncio]>=2.0.30
alembic>=1.13
asyncpg>=0.29          # PostgreSQL (production)
aiosqlite>=0.20        # SQLite fallback (development/tests only)

redis>=5.0
argon2-cffi>=23.1
PyJWT>=2.8

python-multipart>=0.0.9  # multipart uploads
openpyxl>=3.1            # XLSX export renderer
beautifulsoup4>=4.12     # HTML parsing for scraper actors (pure-python)
jinja2>=3.1              # operator UI templates (scraping pages)

# --- test/tooling ---
pytest>=8.2
pytest-asyncio>=0.23
httpx>=0.27
